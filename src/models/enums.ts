export enum ERequestStatus {
    ERROR = "ERROR",
    IDLE = "IDLE",
    PENDING = "PENDING",
    SUCCESS = "SUCCESS",
}

export enum EStoryType {
    NEW = 'new',
    BEST = 'best',
    TOP = 'top'
}
